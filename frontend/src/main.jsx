import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// this is where React starts and puts the app into index.html
ReactDOM.createRoot(document.getElementById("root")).render(<App />);
